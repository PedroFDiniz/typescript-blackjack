{
  "name": "typescript-blackjack",
  "version": "1.0.0",
  "description": "the Blackjack game, but to be run with node on a terminal.",
  "keywords": [
    "game",
    "typescript",
    "node",
    "cmd"
  ],
  "license": "ISC",
  "author": "Pedro Falcão",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --experimental-strip-types --watch --env-file=.env index.ts"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "typescript": "^5.8.3"
  }
}
